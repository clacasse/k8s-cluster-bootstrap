[defaults]
inventory = inventory.ini
roles_path = roles
stdout_callback = yaml
# Host key checking is enabled (Ansible default). Before first run, authorize
# each node's host key with: ssh-keyscan <host> >> ~/.ssh/known_hosts

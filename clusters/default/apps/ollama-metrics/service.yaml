apiVersion: v1
kind: Service
metadata:
  name: ollama-metrics
  labels:
    app: ollama-metrics
spec:
  type: ClusterIP
  selector:
    app: ollama-metrics
  ports:
    - name: proxy
      port: 11434
      targetPort: 8080
    - name: metrics
      port: 8080
      targetPort: 8080
